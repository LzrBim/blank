{% macro status(status) %}

  {% if status == 'active'  or status == 'success' %}
  
  	{% set class = 'success' %}
    
  {% elseif status == 'deleted' or  status == 'banned' %}
  
  	{% set class = 'danger' %}
  
  {% endif %}

	<div class="label label-{{ class }}">{{status|capitalize}}</{{ tag }}>

{% endmacro %}